// @vd/platform — Platform wrappers seam (ARCHITECTURE §4, §seam5): native/web capability shims.
// May import @vd/core only. ui never imports platform. Populated in P0.4.

export const PLATFORM_PACKAGE = '@vd/platform' as const;

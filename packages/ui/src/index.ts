// @vd/ui — component library (ARCHITECTURE §4): may import @vd/theme and @vd/core only
// (never api/voice/platform). Hosts MiraPresence (§10.3). Populated from P0.2 onward.

export const UI_PACKAGE = '@vd/ui' as const;

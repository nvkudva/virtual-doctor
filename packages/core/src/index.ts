// @vd/core — leaf package (ARCHITECTURE §4): Zod schemas, shared types, i18n catalog.
// Depends on nothing internal. Populated in later phases (P0.3).

export const CORE_PACKAGE = '@vd/core' as const;

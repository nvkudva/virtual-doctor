{
  "//": "P0.5: browser SPA. Vite owns the build; tsc is typecheck-only. DOM libs + react-jsx (base is DOM-free by design).",
  "extends": "@vd/config/tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "types": ["vite/client"]
  },
  "include": ["src", "vite.config.ts"],
  "//exclude": "sw.ts is WebWorker scope — typechecked by tsconfig.worker.json, not here.",
  "exclude": ["src/sw.ts"]
}
